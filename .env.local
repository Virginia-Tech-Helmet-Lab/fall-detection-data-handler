# Local Production Environment Configuration
# This file is for local deployment with data persistence

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=local-secret-key-change-if-exposing-externally
JWT_SECRET_KEY=local-jwt-secret-key-change-if-exposing-externally

# Database Configuration - Using PostgreSQL for data safety
DB_USER=falldetection
DB_PASSWORD=localpassword123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=falldetection_local

# Admin User Configuration
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@local.falldetection
ADMIN_PASSWORD=admin123

# CORS Configuration - Allow local network access
CORS_ORIGINS=http://localhost:3000,http://localhost:80,http://127.0.0.1:3000,http://127.0.0.1:80

# File Upload Configuration
UPLOAD_FOLDER=/home/ethan/Documents/GITHUB/fall-detection-data-handler/uploads
MAX_CONTENT_LENGTH=10737418240  # 10GB in bytes

# Backup Configuration
BACKUP_RETENTION_DAYS=30